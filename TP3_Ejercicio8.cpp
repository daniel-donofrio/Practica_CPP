#include <iostream>
using namespace std;

int main() {
    int dia, mes;
    cout << "Ingrese el día: ";
    cin >> dia;
    cout << "Ingrese el mes: ";
    cin >> mes;

    if (dia <= 0 || dia > 31 || mes <=0 || mes > 12) {
        cout << "Ingrese un una fecha válida."
    }

    if (mes == 2) {
        
    }



}